kind = "web"
previewPath = "/"
title = "Halls Head Cricket Club"
version = "1.0.0"
id = "artifacts/cricket-club"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24624

[services.development]
run = "pnpm --filter @workspace/cricket-club run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/cricket-club", "run", "build" ]
publicDir = "artifacts/cricket-club/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24624"
BASE_PATH = "/"
